# Hangman (Console)

A simple console Hangman game implemented in Python.

## How to run
```bash
python3 hangman.py --word repository
# or hide the word from the player who looks at the screen:
python3 hangman.py --secret
# or let it pick a random word from a small built-in list:
python3 hangman.py
